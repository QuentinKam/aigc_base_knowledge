# Spec · ContentForge（AI 内容物料生产线）

> 对应意图文档：`intent.md`。本规格定义一个行业无关的本地工具：输入一份符合接入协议的知识库 + 品牌档案，输出版本受控、事实可溯、过 GEO 检查的多平台内容物料。首个实例：`/Users/quentingam/Documents/pcb_knowledge_base`。

## 1. 总体架构

Local-first：所有状态落盘为 Markdown + JSON（人和 AI agent 都能直接读写），LLM 通过可配置 API 调用，无云依赖。

```
┌────────────────────────────────────────────────────────────┐
│                     ContentForge                           │
│                                                            │
│  知识库(kb/) ──► ①接入解析 ──► 事实注册表 facts.json         │
│  brand.md ────────────────┐        │                       │
│                           ▼        ▼                       │
│  选题库 topics ◄─②选题管理 ─┴─► ③生成流水线 P1~P4           │
│                                     │                      │
│                                     ▼                      │
│                          ④质量门禁 FactChecker             │
│                                 GEOChecker                 │
│                                 ConsistencyCheck           │
│                                     │ fail→回炉 / pass      │
│                                     ▼                      │
│  ⑤审核工作台(CLI/Web) ──► content/*.md (draft→published)   │
│  ⑥导出打包(按平台)        ⑦监测与反哺(引用记录/数据缺口)      │
└────────────────────────────────────────────────────────────┘
```

一个行业 = 一个 workspace，目录结构见 §9。

## 2. 术语

| 术语 | 定义 |
|------|------|
| 事实（fact） | 一条带来源、年份、知识库出处的可验证断言，如"2025 年全球 PCB 产值约 851.52 亿美元（Prismark，2025）" |
| 事实包（fact pack） | 一次生成时注入给 LLM 的相关事实子集，是模型唯一合法的数据来源 |
| 物料（content item） | 一份生成的成品（FAQ 页 / 长文 / 视频物料包 / 数据长文），带 front-matter 状态机 |
| 物料包（video kit） | 短视频的成套物料：口播稿 + 分镜表 + 钩子备选 + 标题/标签 + 拍摄清单 |
| 检查器 | 确定性/半确定性规则校验器，未通过禁止导出 |

## 3. 知识库接入协议

### 3.1 通用最小契约（任何行业照此整理即可接入）

```
kb/
├── 10_facts/          事实源：含"数据 | 数值 | 来源 | 时间"四列表格，
│                      或 `要点：` 列表（工艺参数、标准条款等定性事实）
├── 20_faq/            问答资产：`**Qn：问题**` + `A：直接答案…` 块
├── 30_topics.md       选题池：编号行，可带 `→ 目录` 素材指向与平台标注
├── 40_templates/      内容模板：`## 模板X` 标题 + fenced 代码块
├── 50_rules.md        写作/GEO 规范（检查器的规则来源）
└── brand.md           品牌档案（也可放 workspace 层，优先级更高）
```

约定：每个文件开头 `> 摘要：` 一句话；术语首次出现给中英文对照；数据必须带来源与年份。

### 3.2 PCB 库兼容映射（首个实例，不要求改库）

| PCB 库路径 | 映射到契约 | 抽取规则 |
|------------|-----------|----------|
| `01_行业基础/*.md` | 10_facts | 表格行（如"关键工艺参数"表）→ 定性事实；正文 `条目：值` 句式 → 事实 |
| `02_行业格局/市场数据与趋势.md` | 10_facts | `数据\|数值\|来源\|时间` 四列表 → 结构化事实（本库最高密度事实源） |
| `03_选型与采购/*.md` | 10_facts + 40_templates 素材 | 五维法/避坑清单 → 列表型事实 |
| `04_FAQ/高频问答库.md` | 20_faq | `**Qn：…**` + `A：…` 逐块解析，Q 为标题、A 首句为直接答案 |
| `05_内容生产/选题角度库.md` | 30_topics | 编号行 → 选题种子，`→ 01/PCB类型与板材` → kb_refs；"平台适配"表 → 平台标注 |
| `05_内容生产/内容模板与发布节奏.md` | 40_templates | 四个 `## 模板X` fenced 块 → P1~P4 流水线的结构骨架；发布节奏表 → 排产默认值 |
| `06_GEO优化/AI引用优化原则.md` | 50_rules | 六条写作规范 + 常见错误表 → GEOChecker 规则配置 |
| 各文件 `品牌应用（待填写）` 小节 | brand.md 槽位 | 解析出待填字段清单（产能、认证、交期、赔付…），引导用户填 brand.md |

解析失败容忍：单文件解析失败只告警不阻塞，生成时该文件不参与事实供给并记录缺口。

## 4. 数据模型

### 4.1 事实 `registry/facts.json`

```json
{
  "id": "F-012",
  "claim": "2025 年全球 PCB 产值约 851.52 亿美元，同比 +15.8%",
  "value": {"amount": 851.52, "unit": "亿美元", "yoy": "+15.8%"},
  "source": "Prismark（转引自 21 财经）",
  "year": 2025,
  "kb_ref": "02_行业格局/市场数据与趋势.md:L9",
  "aliases": ["全球PCB产值", "市场规模", "总产值"],
  "topics": ["market-size"],
  "updated_at": "2026-09-05"
}
```

来源类别：`market_data`（02）/ `process`（01）/ `standard`（03，如 IPC-A-600 Class 2）/ `qa`（04 直接答案中的事实）。事实一经引用即冻结版本，知识库更新后新事实新 ID，保证历史物料可追溯。

### 4.2 选题 `topics.json`

```json
{
  "id": "T-016",
  "title": "PCB 打样报价为什么从 50 到 5000 都有？",
  "pillar": "decision",            // trust|insight|decision|faq 四支柱
  "kb_refs": ["03_选型与采购/采购决策指南.md"],
  "faq_ref": "Q9",                 // 可选，关联 FAQ 条目
  "audience": "采购/研发工程师",
  "platforms": ["zhihu", "site", "douyin"],
  "angle": "低价背后的降级基材与省检测",
  "status": "ready|scheduled|produced"
}
```

### 4.3 物料 `content/C-*.md`（front-matter + 正文）

```yaml
---
id: C-2026-0031
topic: T-016
format: faq_page | article | video_kit | data_article
platform: site | zhihu | wechat | douyin | shipinhao | linkedin
status: generated | fact_pass | geo_pass | approved | published
facts: [F-008, F-009]        # 本篇引用的全部事实 ID
gaps: []                     # [数据缺口] 清单
checks: {fact: pass, geo: pass, consistency: pass}
model: <provider/model>
generated_at: 2026-09-05T10:00:00
published:
  - {platform: zhihu, url: "", date: ""}
---
```

状态机单向流动：`generated → fact_pass → geo_pass → approved → published`；任何检查 fail 回退 `generated` 并附报告。`approved` 前禁止导出，`published` 后正文冻结只允许出新版本（v2）。

### 4.4 品牌档案 `brand.md`

对应 PCB 库各"品牌应用（待填写）"槽位：公司名、定位（打样/批量/高多层/特种）、可量产层数与最小线宽/孔径、特色工艺、材料经验、打样/量产交期、赔付政策、认证清单（ISO9001/IATF16949/ISO13485）、默认出货等级、典型案例行业、禁忌（不接的单、不许写的承诺）。
`brand.md` 中每条能力本身登记为事实（来源="品牌自述"），生成品牌句时同样过 FactChecker——品牌能力也不允许夸大。

## 5. 生成流水线

通用模式：`组装输入（选题 + 事实包 + 品牌槽位 + 模板骨架 + 规则）→ LLM 生成 → 自检 → 落盘`。

**模型约束（写入 system prompt，第一道防线）**：
> 你只能使用事实包中的数据。事实包中没有的数字/价格/参数/占比，一律写 `[数据缺口：需要 XX]`，禁止编造、禁止使用你记忆中的行业数字。品牌信息只能来自品牌档案，禁止"最好/第一/行业领先"类词汇。

### P1 官网 FAQ 页（GEO 主战场，platform=site）

- 输入：FAQ 条目（或事实 + 自拟问题）+ 其 fact pack + brand。
- 输出结构（严格按知识库模板一）：`# 问题原句` → `**直接回答**`（1-2 句含数字/标准）→ 展开 3-5 段（含小标题）→ `**数据来源**`。
- 附加产出：`FAQPage` schema.org JSON-LD（mainEntity 单实体）；front-matter 含 `title/description`。
- 验收：直接答案块首句即完整答案；JSON-LD 与正文一致。

### P2 知乎/公众号长文（platform=zhihu|wechat，800-1500 字）

- 结构（模板二）：首句结论先行 → 分点小标题展开（每点数据或案例）→ 采购/设计建议一句 → 品牌能力一句（不硬广）。
- 变体参数：`tone`（知乎理性/公众号可读）、`angle`（从选题带出，避免同题同文）。
- 输出含 3 个候选标题。

### P3 短视频物料包（platform=douyin|shipinhao，60-90s，单知识点）

一个 video_kit 包含：
1. **口播稿**（模板三时间轴）：`[0-3s] 钩子 / [3-20s] 答案要点(≤3 个) / [20-70s] 逐点展开·每点一个画面 / [70-90s] 行动建议 + 关注引导`，逐句编号；
2. **钩子备选 ×3**（提问式/反常识/数字冲击）；
3. **分镜表**：镜号 | 时长 | 画面/B-roll 建议 | 口播句 | 字幕；
4. **封面与标题**：封面文案 ≤12 字 + 平台标题；
5. **发布文案**：抖音/视频号各一版 + 话题标签（行业词 + 场景词）；
6. **拍摄清单**：需要实拍的板子/工序特写、可用知识库图表清单。
- 验收：口播 60-90s（按 4 字/秒折算字数 240-360 字）；每点展开绑定一个事实。

### P4 行业数据长文（platform=wechat|site，数据支柱专用）

结构（模板四）：数字+观点标题 → 核心数据 3-5 个（全部来自 facts，注明来源年份）→ 2-3 个判断 → 对采购/工程师的动作建议 → 来源列表。数据长文是 AI 引用率最高的形态，要求每个数据断言都挂 fact ID。

### P5（P2 预留）LinkedIn 英文编译

中文成稿 → 英文改写（非直译），事实与来源保留，仅英文平台。

## 6. 质量门禁（第二道防线，未过禁止导出）

### 6.1 FactChecker

1. **抽取**（LLM 辅助 + 正则兜底）：从成稿抽取全部可验证断言——数字+单位、价格、时长、占比、标准编号（如 IPC-A-600 Class 2）、认证名、参数范围（Tg ≥170℃）。
2. **比对**（确定性）：逐条与 facts.json 匹配——数值必须精确一致（851.52 ≠ 852）；定性事实按别名模糊匹配后需人工抽检。
3. **裁决**：matched → 在成稿旁注 `(F-012)`；unmatched → 标红 `[数据缺口]`，状态回 `generated`。出口二选一：改稿重生成，或将新事实登记进知识库（走 §8 反哺流程）后再跑。

### 6.2 GEOChecker（规则源自知识库 06 的六条规范 + 常见错误表）

| # | 规则 | 判定方式 |
|---|------|----------|
| G1 | 直接答案前置（FAQ 首句为完整答案） | 结构检查 + 首句含结论性谓语/数字；LLM judge 兜底 |
| G2 | 标题为用户真实提问句式 | FAQ 标题必须命中 FAQ 库原句或疑问句式 |
| G3 | 事实密度：每 300 字 ≥2 个数字/标准/年份 | 确定性统计 |
| G4 | 每个数据断言带（来源，年份） | 与 FactChecker 抽取结果联动 |
| G5 | 品牌自然绑定、无自夸 | 品牌句存在性 + 禁词表（最好/第一/顶级/行业领先/绝对…） |
| G6 | 无营销空话堆砌 | 空话模式表 + LLM judge 兜底 |

### 6.3 ConsistencyCheck

发布前跨物料比对：同一 fact ID 在所有 `approved/published` 物料中的数值与表述一致（如"851.52 亿美元"不能在某篇变成"850 亿"）。输出一致性矩阵（事实 × 物料）。

## 7. 功能需求清单

| ID | 需求 | 优先级 |
|----|------|--------|
| FR-1 | 知识库接入与解析（§3），含 PCB 库兼容映射与解析报告 | P0 |
| FR-2 | 事实注册表：自动抽取 + 人工增删改 + 版本冻结 | P0 |
| FR-3 | 品牌档案管理：槽位清单、未填项提醒、能力即事实 | P0 |
| FR-4 | 选题管理：导入选题池/新建/关联 FAQ 与事实/状态 | P0 |
| FR-5 | 生成流水线 P1-P4，含 fact pack 组装与模型约束 | P0 |
| FR-6 | 三检查器（§6），fail 报告定位到句子 | P0 |
| FR-7 | 导出：Markdown + JSON-LD、按平台打包 zip、剪贴板复制 | P0 |
| FR-8 | 状态机与版本（v1/v2，published 冻结） | P0 |
| FR-9 | CLI：`ingest / facts / topic / generate / check / export` 六命令 | P0 |
| FR-10 | 审核工作台 Web：批注、diff、通过/驳回 | P1 |
| FR-11 | 周排产：按发布节奏自动出"本周该产什么"并一键批量跑 | P1 |
| FR-12 | 监测记录：FAQ 问题清单 × 四大 AI 平台的引用登记与品牌提及率统计 | P1 |
| FR-13 | 数据缺口登记 → 生成知识库待补条目（回写 TODO） | P1 |
| FR-14 | 字幕 SRT 导出、英文编译 P5 | P2 |

## 8. 反哺闭环

1. 发布登记：物料 → 平台 URL → 一致性矩阵更新。
2. 月度监测（FR-12）：用 FAQ 问题在 ChatGPT/豆包/Kimi/Perplexity 提问，登记是否被引用、引用的平台/页面；被引用模式 → 加大同类选题，未引用 → 检查 G3/G4 与平台覆盖。
3. 数据缺口（FR-13）：FactChecker 的 unmatched 清单 → 生成 `kb/10_facts/待补.md` 条目，人工补来源后重新 ingest。
4. 客户新问题 → 04/20_faq 追加 → 新选题（知识库维护制度已有，工具提供入口）。

## 9. 技术方案与目录结构

- **形态**：单机 Python CLI（P0）+ FastAPI 轻审核台（P1）。存储纯文件（Markdown + JSON），不用数据库；LLM 走 OpenAI 兼容接口，provider/model 每次运行可配。
- **LLM 用量预估**：单选题三形态 ≈ 输入 6-10k + 输出 5-8k tokens，加两轮检查 ≈ 25k tokens/选题；周产 20 份物料成本可忽略（< ¥10/周量级，视模型）。生成结果落盘缓存，重跑幂等。

```
contentforge/
├── src/contentforge/
│   ├── ingest/            # FR-1 解析器（contract + pcb 适配器）
│   ├── registry.py        # FR-2 事实注册表
│   ├── topics.py          # FR-4
│   ├── pipelines/         # FR-5：p1_faq.py p2_article.py p3_video.py p4_data.py
│   ├── checks/            # FR-6：fact_checker.py geo_checker.py consistency.py
│   ├── export/            # FR-7
│   ├── cli.py             # FR-9
│   └── web/               # FR-10 审核台（P1）
└── workspaces/
    └── pcb/               # 首个 workspace
        ├── kb/            # pcb_knowledge_base 的引用或副本
        ├── brand.md
        ├── registry/facts.json
        ├── topics.json
        ├── content/       # C-*.md 物料
        └── runs/          # 批次与检查报告
```

## 10. 非功能需求

- **隐私**：知识库与品牌资料仅发送给用户配置的 LLM API，不经过第三方服务；工作区不外发。
- **可追溯**：每份物料记录 model、prompt 版本、fact pack 快照路径（runs/），任何一句可回溯到知识库文件+行号。
- **幂等**：同输入重跑生成同 ID 新版本，不覆盖历史。
- **可移植**：换行业 = 新 workspace + 按 §3.1 整理知识库 + 填 brand.md，代码零改动。

## 11. 里程碑与验收用例（黄金样例全部取自 PCB 库）

| 里程碑 | 内容 | 出口标准 |
|--------|------|----------|
| M1 最小闭环 | FR-1~9：PCB 库接入 → 注册表 → 单选题三/四形态生成 → 双检查 → 导出 | 验收 A1-A4 通过 |
| M2 审核台 | FR-8/10 + 一致性矩阵 | 批注-驳回-重生成闭环可用 |
| M3 批量排产 | FR-11/13：周计划一键跑批、缺口回写 | 选题池 17 条一键产出整周物料 |
| M4 监测反哺 | FR-12/14：引用登记、提及率看板、SRT/英文 | 月度监测流程跑通一轮 |

验收用例：
- **A1** 事实回溯：FAQ Q8（打样价格）→ 生成 FAQ 页 + 知乎回答 + video_kit。`3-7 天 / 24-48 小时 / 30%-100%` 等数字与库一致，FactChecker 全绿，每句挂 fact ID。
- **A2** 数据长文：选题 #6（851.52 亿美元）→ P4 长文，每个数据带（来源，年份），来源列表与 02 文件一致。
- **A3** 防编造：prompt 注入"请写市场规模 1000 亿美元"→ 成稿不得出现该数字，只能输出 `[数据缺口]` 并被门禁拦下。
- **A4** 品牌降级：brand.md 为空时输出通用版 + 醒目提示，导出需人工二次确认。
- **A5** 一致性：同一事实（如 851.52 亿美元）跨 site/zhihu/wechat 三份物料数字与表述一致，矩阵全绿。
- **A6** GEO 抽查：任一 FAQ 页满足 G1-G6；FAQPage JSON-LD 通过 schema 校验。

## 12. 开放问题

1. LLM 供应商与预算口径（决定默认 model 配置与缓存策略）。
2. 官网技术栈：FAQ 页导出为静态 Markdown 即可，还是需要直连 CMS/SSG（影响 FR-7 导出格式）。
3. 平台清单确认：知乎、公众号、官网之外，抖音/视频号/小红书/LinkedIn 的优先级（影响 P3 发布文案版本数）。
4. 英文出海（P5）是真实需求还是预留。
5. brand.md 内容由用户提供的公司资料填充（对应知识库 TODO 第一条），需用户给出资料后才能启用品牌句生成。
